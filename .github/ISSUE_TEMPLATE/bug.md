---
name: Ложное срабатывание или пропущенный отказ
about: Сторож переключил зря или не переключил вовремя
---

**Что произошло** (адреса можно заменить на 203.0.113.x):

**Вывод `bridge-guard --dry-run`:**

```
```

**Сообщение сторожа в Telegram (если было):**

**Конфиг без токенов** (`bridges`, `probe.port`, `thresholds`, `failover`):

```json
```

**Версии:** bridge-guard `--version`, Xray-core `xray version`, Remnawave.
